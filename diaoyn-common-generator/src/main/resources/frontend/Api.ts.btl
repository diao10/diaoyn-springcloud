export interface ${classNameCamelCase}Dto {
    <% for(var i = 0; i < configList.~size; i++) { %>
    ${configList[i].fieldNameCamelCase}: ${configList[i].fieldJavaType};  //${configList[i].fieldComment}
	<% } %>
}