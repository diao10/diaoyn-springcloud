export interface ${classNameCamelCase}Dto {
    <% for(var i = 0; i < configList.~size; i++) { %>
    ${configList[i].fieldNameCamelCase}: ${configList[i].fieldTsType};  //${configList[i].fieldComment}
	<% } %>
}